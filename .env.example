# Example environment variables for local development
# Copy this to `.env` or export the values in your shell. Do NOT commit secrets.

# Twitch App credentials (from https://dev.twitch.tv/console/apps)
TWITCH_CLIENT_ID=mj62akob65zw3vd5oqzl9qtnbvyhj2
TWITCH_CLIENT_SECRET=<your-client-secret-here>

# Redirect URI used when registering the app for Authorization Code flow
# If you registered https://127.0.0.1:3883, use that here when performing the
# OAuth dance locally.
TWITCH_OAUTH_REDIRECT_URI=https://127.0.0.1:3883

# Optional: allow overriding the alert server used by the API helper
# ALERTS_URL=http://127.0.0.1:3080/trigger

# The primary channel the bot will join (login name, all lowercase usually)
TWITCH_CHANNEL=storygirl
